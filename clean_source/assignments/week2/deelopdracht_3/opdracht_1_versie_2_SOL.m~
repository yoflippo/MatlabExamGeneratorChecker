%% Opdracht 1
% Maak een functie aan met de functienaam maxMin waarbij je de
% maximale waarde en minimale waarde krijgt als output. de input is en
% vector.
% 

function [maximaleWaarde, minimaleWaarde]= maxMin(hoek)

cosHoek=cos(hoek);
sinHoek=sin(hoek);

end

