% add files and folders
addpath(genpath(pwd));
basedir = 'copyfiles_directory';

% find all m-files
files = readFilesInSubFolder(basedir,'.m');
files{1}
files{end}

% read empty folder
basedir = 'emptyfolder';
files = readFilesInSubFolder(basedir,'.m');